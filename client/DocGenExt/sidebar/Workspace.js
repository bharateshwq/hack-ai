const searchWorkspace = async (query) => {
    console.log(`[Workspace] Searching for: ${query}`);
    return [];
};

const findSymbols = async (query) => {
    console.log(`[Workspace] Finding symbols: ${query}`);
    return [];
};

module.exports = {
    searchWorkspace,
    findSymbols
};
