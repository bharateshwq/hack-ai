package com.bharatesh.rag_trial.dto.RegistryEntityDTO;

public enum MaskingType {
    MASK,       // Mask the sensitive value (e.g., replace with ****)
    REDACT      // Remove the sensitive value entirely
}
